{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "全部应用窗口（主界面、各屏圈选覆盖层、穿透标记框）的默认权限：自定义 invoke 命令与事件监听",
  "windows": ["main", "overlay-*", "mark"],
  "permissions": [
    "core:default"
  ]
}
